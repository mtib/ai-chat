# OpenAI API Key (optional - can also be set via UI)
VITE_OPENAI_API_KEY=your_api_key_here

# Build version for cache versioning (optional - defaults to timestamp if not set)
# Can be a git hash, version number, or any unique identifier
VITE_BUILD_VERSION=
